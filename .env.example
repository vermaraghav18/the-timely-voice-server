# Server
PORT=4000
NODE_ENV=production

# CORS (comma-separated, no spaces). Fill these after Vercel deploys.
ALLOWED_ORIGINS=https://your-client.vercel.app,https://your-admin.vercel.app

# Sessions / JWT
SESSION_SECRET=replace-with-a-long-random-string
JWT_SECRET=replace-with-another-long-random-string

# Database (use Postgres on Render; we'll create it in Step 2)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
EOF